# --- dependencies ---
node_modules/
.venv/
venv/
__pycache__/
*.py[cod]

# --- env & secrets ---
.env
.env.local
.env.*.local
!.env.example

# API key Vast.ai dalam teks polos — JANGAN pernah commit.
# Simpan di .env (di atas) atau di luar repo (~/.vastai, Credential Manager).
api-JernihAI*.txt
*.vastai-key*

# --- next.js ---
.next/
out/

# --- python ---
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# --- uploads & hasil (privasi — FR-07, jangan pernah commit gambar user) ---
api/storage/

# --- model weights runtime (FR-08/FR-09) — diunduh download_models.py,
#     ter-bake di image worker saat build; jangan commit file .pth ---
api/gfpgan/
api/facexlib/

# --- misc ---
.DS_Store
*.log
npm-debug.log*

# TypeScript incremental build cache
*.tsbuildinfo
